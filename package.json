{
  "name": "vgateway",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Static Reveal.js deck about MCP Gateway architecture.",
  "scripts": {
    "serve": "http-server . -c-1 -o index.html",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "audit": "npm audit --audit-level=moderate"
  },
  "dependencies": {
    "bootstrap-icons": "^1.13.1",
    "mermaid": "^11.16.0",
    "reveal.js": "^6.0.1"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^4.1.10",
    "http-server": "^14.1.1",
    "vitest": "^4.1.10"
  }
}
